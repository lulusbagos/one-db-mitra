@model IReadOnlyList<one_db_mitra.Models.Admin.PositionListItem>
@{
    ViewData["Title"] = "Manajemen Jabatan";
    var companyOptions = ViewBag.CompanyOptions as IEnumerable<SelectListItem> ?? Enumerable.Empty<SelectListItem>();
    var guideItems = new[]
    {
        "Pastikan section sudah dibuat sebelum menambah jabatan.",
        "Gunakan filter perusahaan untuk mempercepat pencarian jabatan.",
        "Jabatan akan dipakai saat input karyawan dan pemetaan akses."
    };
}

<div class="container-fluid px-0">
    <div class="d-flex flex-column flex-lg-row justify-content-between align-items-start align-items-lg-center gap-3 mb-4">
        <div>
            <p class="text-uppercase text-secondary small mb-1">Pengaturan</p>
            <h2 class="mb-1">Manajemen Jabatan</h2>
            <p class="text-secondary mb-0">Kelola jabatan di tiap section.</p>
        </div>
        <div class="d-flex flex-wrap gap-2 align-items-center">
            <form method="get" class="d-flex flex-wrap align-items-center gap-2">
                <input type="text" class="form-control form-control-sm" name="search" placeholder="Cari jabatan" value="@(ViewBag.CurrentSearch ?? string.Empty)" />
                <select class="form-select form-select-sm" name="companyId" asp-items="companyOptions"></select>
                <input type="hidden" name="activeOnly" value="false" />
                <div class="form-check">
                    <input class="form-check-input" type="checkbox" id="positionActiveOnly" name="activeOnly" value="true" @(((bool?)ViewBag.ActiveOnly ?? true) ? "checked" : null) onchange="this.form.submit()" />
                    <label class="form-check-label" for="positionActiveOnly">Tampilkan aktif saja</label>
                </div>
                <span class="badge @(((bool?)ViewBag.ActiveOnly ?? true) ? "text-bg-success" : "text-bg-secondary")">
                    @(((bool?)ViewBag.ActiveOnly ?? true) ? "Filter: Aktif" : "Filter: Semua")
                </span>
                <button type="submit" class="btn btn-sm btn-outline-primary">Cari</button>
            </form>
            @if (ViewBag.ScopeInfo is string scopeInfo && !string.IsNullOrWhiteSpace(scopeInfo))
            {
                <span class="badge text-bg-light border">
                    <i class="bi bi-shield-lock me-1"></i>@scopeInfo
                </span>
            }
            <a class="btn btn-primary" asp-action="CreatePosition">
                <i class="bi bi-plus-lg me-2"></i>Tambah Jabatan
            </a>
        </div>
    </div>
    @await Html.PartialAsync("_SettingGuide", guideItems, new ViewDataDictionary(ViewData)
    {
        ["GuideTitle"] = "Cara mengatur jabatan"
    })

    <div class="card">
        <div class="card-header">
            <h6 class="mb-0">Daftar Jabatan</h6>
        </div>
        <div class="card-body table-responsive">
            <table class="table table-striped align-middle mb-0">
                <thead>
                    <tr>
                        <th>Kode</th>
                        <th>Nama</th>
                        <th>Perusahaan</th>
                        <th>Section</th>
                        <th>Status</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Count == 0)
                    {
                        <tr>
                            <td colspan="6" class="text-center text-secondary py-4">Belum ada jabatan. Tambahkan jabatan pertama.</td>
                        </tr>
                    }
                    else
                    {
                        @foreach (var position in Model)
                        {
                            <tr>
                                <td>@position.PositionCode</td>
                                <td>@position.PositionName</td>
                                <td>@position.CompanyName</td>
                                <td>@position.SectionName</td>
                                <td>
                                    <span class="badge @(position.IsActive ? "text-bg-success" : "text-bg-secondary")">
                                        @(position.IsActive ? "Aktif" : "Nonaktif")
                                    </span>
                                </td>
                                <td class="text-end">
                                    <a class="btn btn-sm btn-outline-primary" asp-action="EditPosition" asp-route-id="@position.PositionId">Edit</a>
                                <form asp-action="DeletePosition" asp-route-id="@position.PositionId" method="post" class="d-inline">
                                    @Html.AntiForgeryToken()
                                    <input type="hidden" name="activeOnly" value="@((bool?)ViewBag.ActiveOnly ?? true)" />
                                    <button type="submit" class="btn btn-sm btn-outline-danger" onclick="return confirm('Hapus jabatan ini?')">Hapus</button>
                                </form>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
