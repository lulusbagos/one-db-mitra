@model IReadOnlyList<one_db_mitra.Models.Admin.DepartmentListItem>
@{
    ViewData["Title"] = "Manajemen Departemen";
    var companyOptions = ViewBag.CompanyOptions as IEnumerable<SelectListItem> ?? Enumerable.Empty<SelectListItem>();
}

<div class="container-fluid px-0">
    <div class="d-flex flex-column flex-lg-row justify-content-between align-items-start align-items-lg-center gap-3 mb-4">
        <div>
            <p class="text-uppercase text-secondary small mb-1">Pengaturan</p>
            <h2 class="mb-1">Manajemen Departemen</h2>
            <p class="text-secondary mb-0">Kelola departemen berdasarkan perusahaan.</p>
        </div>
        <div class="d-flex flex-wrap gap-2 align-items-center">
            <form method="get" class="d-flex flex-wrap align-items-center gap-2">
                <input type="text" class="form-control form-control-sm" name="search" placeholder="Cari departemen" value="@(ViewBag.CurrentSearch ?? string.Empty)" />
                <select class="form-select form-select-sm" name="companyId" asp-items="companyOptions"></select>
                <input type="hidden" name="activeOnly" value="false" />
                <div class="form-check">
                    <input class="form-check-input" type="checkbox" id="departmentActiveOnly" name="activeOnly" value="true" @(((bool?)ViewBag.ActiveOnly ?? true) ? "checked" : null) onchange="this.form.submit()" />
                    <label class="form-check-label" for="departmentActiveOnly">Tampilkan aktif saja</label>
                </div>
                <span class="badge @(((bool?)ViewBag.ActiveOnly ?? true) ? "text-bg-success" : "text-bg-secondary")">
                    @(((bool?)ViewBag.ActiveOnly ?? true) ? "Filter: Aktif" : "Filter: Semua")
                </span>
                <button type="submit" class="btn btn-sm btn-outline-primary">Cari</button>
            </form>
            @if (ViewBag.ScopeInfo is string scopeInfo && !string.IsNullOrWhiteSpace(scopeInfo))
            {
                <span class="badge text-bg-light border">
                    <i class="bi bi-shield-lock me-1"></i>@scopeInfo
                </span>
            }
            <a class="btn btn-primary" asp-action="CreateDepartment">
                <i class="bi bi-plus-lg me-2"></i>Tambah Departemen
            </a>
        </div>
    </div>

    <div class="card">
        <div class="card-header">
            <h6 class="mb-0">Daftar Departemen</h6>
        </div>
        <div class="card-body table-responsive">
            <table class="table table-striped align-middle mb-0">
                <thead>
                    <tr>
                        <th>Kode</th>
                        <th>Nama</th>
                        <th>Perusahaan</th>
                        <th>Status</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Count == 0)
                    {
                        <tr>
                            <td colspan="5" class="text-center text-secondary py-4">Belum ada departemen. Tambahkan departemen pertama.</td>
                        </tr>
                    }
                    else
                    {
                        @foreach (var dept in Model)
                        {
                            <tr>
                                <td>@dept.DepartmentCode</td>
                                <td>@dept.DepartmentName</td>
                                <td>@dept.CompanyName</td>
                                <td>
                                    <span class="badge @(dept.IsActive ? "text-bg-success" : "text-bg-secondary")">
                                        @(dept.IsActive ? "Aktif" : "Nonaktif")
                                    </span>
                                </td>
                                <td class="text-end">
                                    <a class="btn btn-sm btn-outline-primary" asp-action="EditDepartment" asp-route-id="@dept.DepartmentId">Edit</a>
                                <form asp-action="DeleteDepartment" asp-route-id="@dept.DepartmentId" method="post" class="d-inline">
                                    @Html.AntiForgeryToken()
                                    <input type="hidden" name="activeOnly" value="@((bool?)ViewBag.ActiveOnly ?? true)" />
                                    <button type="submit" class="btn btn-sm btn-outline-danger" onclick="return confirm('Hapus departemen ini?')">Hapus</button>
                                </form>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
