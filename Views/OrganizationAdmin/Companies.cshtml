@model IReadOnlyList<one_db_mitra.Models.Admin.CompanyListItem>
@{
    ViewData["Title"] = "Manajemen Perusahaan";
    var guideItems = new[]
    {
        "Pengajuan perusahaan dilakukan melalui menu Pengajuan Perusahaan.",
        "Gunakan filter aktif untuk fokus pada perusahaan yang masih berlaku.",
        "Data perusahaan lama yang belum lengkap akan diberi tanda merah."
    };
}

<div class="container-fluid px-0">
    <div class="d-flex flex-column flex-lg-row justify-content-between align-items-start align-items-lg-center gap-3 mb-4">
        <div>
            <p class="text-uppercase text-secondary small mb-1">Pengaturan</p>
            <h2 class="mb-1">Manajemen Perusahaan</h2>
            <p class="text-secondary mb-0">Daftar perusahaan berdasarkan hirarki dan tipe.</p>
        </div>
        <div class="d-flex flex-wrap gap-2 align-items-center">
            <form method="get" class="d-flex flex-wrap align-items-center gap-2">
                <input type="text" class="form-control form-control-sm" name="search" placeholder="Cari perusahaan" value="@(ViewBag.CurrentSearch ?? string.Empty)" />
                <input type="hidden" name="activeOnly" value="false" />
                <div class="form-check">
                    <input class="form-check-input" type="checkbox" id="companyActiveOnly" name="activeOnly" value="true" @(((bool?)ViewBag.ActiveOnly ?? true) ? "checked" : null) onchange="this.form.submit()" />
                    <label class="form-check-label" for="companyActiveOnly">Tampilkan aktif saja</label>
                </div>
                <span class="badge @(((bool?)ViewBag.ActiveOnly ?? true) ? "text-bg-success" : "text-bg-secondary")">
                    @(((bool?)ViewBag.ActiveOnly ?? true) ? "Filter: Aktif" : "Filter: Semua")
                </span>
                <button type="submit" class="btn btn-sm btn-outline-primary">Cari</button>
            </form>
            @if (ViewBag.ScopeInfo is string scopeInfo && !string.IsNullOrWhiteSpace(scopeInfo))
            {
                <span class="badge text-bg-light border">
                    <i class="bi bi-shield-lock me-1"></i>@scopeInfo
                </span>
            }
        </div>
    </div>
    @await Html.PartialAsync("_SettingGuide", guideItems, new ViewDataDictionary(ViewData)
    {
        ["GuideTitle"] = "Cara mengatur perusahaan"
    })

    <div class="card">
        <div class="card-header">
            <h6 class="mb-0">Daftar Perusahaan</h6>
        </div>
        <div class="card-body table-responsive">
            <table class="table table-striped align-middle mb-0">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Kode</th>
                        <th>Nama</th>
                        <th>Tipe</th>
                        <th>Induk</th>
                        <th>Status</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Count == 0)
                    {
                        <tr>
                            <td colspan="7" class="text-center text-secondary py-4">Belum ada data perusahaan.</td>
                        </tr>
                    }
                    else
                    {
                        @foreach (var company in Model)
                        {
                            <tr>
                                <td>@company.CompanyId</td>
                                <td>@company.CompanyCode</td>
                                <td>
                                    <div class="fw-semibold">@company.CompanyName</div>
                                    @if (company.DokumenBelumLengkap)
                                    {
                                        <div class="small text-danger">@company.RemarkDokumen</div>
                                    }
                                </td>
                                <td>
                                    <div class="fw-semibold">@company.CompanyTypeName</div>
                                    <div class="small text-secondary">ID: @(company.CompanyTypeId?.ToString() ?? "-")</div>
                                </td>
                                <td>@company.ParentCompanyName</td>
                                <td>
                                    <span class="badge @(company.IsActive ? "text-bg-success" : "text-bg-secondary")">
                                        @(company.IsActive ? "Aktif" : "Nonaktif")
                                    </span>
                                </td>
                                <td class="text-end">
                                    <a class="btn btn-sm btn-outline-primary" asp-action="EditCompany" asp-route-id="@company.CompanyId">Edit</a>
                                <form asp-action="DeleteCompany" asp-route-id="@company.CompanyId" method="post" class="d-inline">
                                    @Html.AntiForgeryToken()
                                    <input type="hidden" name="activeOnly" value="@((bool?)ViewBag.ActiveOnly ?? true)" />
                                    <button type="submit" class="btn btn-sm btn-outline-danger" onclick="return confirm('Hapus perusahaan ini?')">Hapus</button>
                                </form>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
